課題を確認しました。
マイルからメートルへの変換(1600倍する)についてOKです。

一方で、メートルからマイルへの変換が実装されていません。
例えば、以下のような実装を期待しています。

```
int meter = 4800;
int mile = /* meterをマイルに変換する */
// meterメートルはmileマイルと表示する
```

この点を加味して再提出をお願いします。


プログラムを実行したところ、以下の結果になりました。

> 5マイルは8000メートルです。
> 4800マイルは3メートルです。

1つ目は正しいのですが、2つ目の結果は誤りですね。

> 4800メートルは３マイルです。

が期待する結果かと思います。

また、プログラムの実装としては、以下のようなものを期待しています。

```
int mile = 5;
int metre = 4800;

// 変数mileを1600倍して表示する
// "mile"マイルは"5*mile"メートルです

// 変数meterを1600で割って表示する
// "meter"メートルは"meter/1600"マイルです
```

この点を踏まえて修正をお願いします。

課題を確認しました、OKです。
本質的には同じですが、このような書き方もできます、参考まで。

```
int mile = 5;
int meter = mile * 1600;
System.out.println(mile + "マイルは" + meter + "メートルです。");
```

レビュー指摘事項の修正を確認しました、OKです。
メートルからマイル、マイルからメートルへの変換、両方とも正しく実装できておりました。
基本の四則演算は問題なしですね。

修正の結果、別のバグが入り込む、というのは現実の業務でもありがちです。
丁寧に動作を確認するくせを付けるとよいですね。

わかりやすい変数名を使うとなお良いです。
例えば以下の通り。

```
int meter_a = 3200;
int mile_a = meter / 1600;

int mile_b = 3;
int meter_b = mile_b * 1600;
```

課題を確認しました、OKです。
かけ算、わり算、文字列出力など、正しく実装できておりました。
変数名もよいですね。
この調子で学習を進めてください。
