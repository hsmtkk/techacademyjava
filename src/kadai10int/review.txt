課題を確認しました、OKです。
レビュー指摘事項の修正を確認しました、OKです。
String配列の作成、for文での繰り返し、intからStringへの変換など正しく実装できています。
この調子で学習を進めてください。

間違いというわけではないのですが、再提出をお願いします。

この課題では、1から100までの数字を __手書きではなくプログラムで__ 作成することがポイントです。

今回は100なので何とか手でも書けるのですが、1000、10000だと非現実的ですよね。
現実の業務ではそういったケースもありうるので、この場で練習しておきましょう。
for文を使い、以下のような枠組みで記述してみてください。

```
		String[] ss = new String[100];
		for (int i = 0; i < ss.length; i++) {
			// 配列ssに数字を代入する。
		}
```

完璧です。

文字列配列の生成、intからStringへの変換、拡張for文の使い方まで、ばっちりですね。

境界値（この問題では0,1, 99, 100, 101など）にはバグが入りがちです。
今後も気を付けていきましょう。

