惜しいです、問題2の再提出をお願いします。

# 問題1

確認しました、OKです。
境界値（この問題では100）にはバグが入りがちです、気を付けていきましょう。

# 問題2

課題の指定は以下の通りです。
> 実行するたびに1-6の整数値をランダムに表示させましょう。

実行したところ、13と表示されてしまいました。
乱数を100倍していますが、ここに誤りがありそうです。

```
int ran = (int) (Math.random() * 100);
```

# 問題3, 問題4
OKです、乱数の繰り返し生成処理を正しく実装できましたね。


*

* 問題2、惜しいです。
課題の指定は1以上6以下の整数値の表示ですが、小数値 ```1.0``` が表示されました。

```
double randomNum =(int) Math.random() * 6;
```

int型にキャストしているのですが、double型で受けているのが原因です。
int型で受けるように直しましょう。

* 問題3、確認しました、OKです。
for文と乱数生成の組み合わせを正しく実装できています。

* 問題4、惜しいです。
何度か試してもらいたいのですが、6の目が出ません。
乱数の生成部分に問題がありそうです。
他の部分はよく書けているので、この点を確認してみてください。

レビュー指摘事項の修正を確認しました、OKです。
これで乱数の生成処理は完璧ですね。
この調子で学習を進めてください。

# 問題1

惜しいです、課題の指定は以下の通りです。
> 0-100の間で整数をランダムに1つ表示させましょう

一方、nextIntの仕様は以下の通りです。
https://docs.oracle.com/javase/jp/8/docs/api/java/util/Random.html#nextInt-int-
> 0から指定された値の範囲(0は含むが、その指定された値は含まない)
となるので、nextInt(100)ですと、「100」を含みません。

# 問題2

課題の指定は以下の通りです。
> 実行するたびに1-6の整数値をランダムに表示させましょう。

実行したところ、8や9も表示されてしまいました。
乱数を10倍していますが、ここに誤りがありそうです。

```
int ran = (int)(Math.random()*10);
```

問題1と同様に境界値(この問題では1や6の前後)に気を付けましょう。

# 問題3

あいにく、課題の意図が正しく伝わらなかったようです。
> ランダムな整数値(1-10)を10回表示しましょう。

以下のような出力になるようにしましょう。

```
回数: 1, 値: 9
回数: 2, 値: 7
(途中省略)
回数: 10, 値: 4
```

またこの問題ではMathクラスの使用が指定されています(Randomクラスではありません)。
Math.randomメソッドを使って実装してみましょう。

# 問題4

完璧です、素晴らしい。
しいて申し上げると、変数名を工夫するとよいですね。
例えば和を保持する変数は```d```よりも```sum```などとした方がわかりやすいです。
