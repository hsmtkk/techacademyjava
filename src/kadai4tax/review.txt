課題を確認しました、OKです。

しいて申しあげると、変数名を工夫するとなお良しです。

```
int netPrice = Integer.parseInt(str); // 税抜金額
int taxValue = tax(netPrice); // 税金
int taxPrice = netPrice + taxValue; // 税込金額
```

実業務では複数名でコーディングをすることが普通です。
その際、処理を適切な単位でメソッドに分割するのが重要になってきます。


処理そのものは正しいのですが、仕様に沿っていない点があります。
再提出をお願いします。

```
mainメソッドとtaxメソッドを切り分けてください。
taxメソッドでは消費税計算(8%)のみ行います。
mainメソッドはtaxメソッドから戻り値の消費税(8%)を受けてください。
```

以上が仕様なので、以下のような作りになるはずです。

```
	    public static void main(String[] args) {
	        int price = 100;
	        int tax = tax(price, 8);
	        // 税込み金額の計算
	        // 税抜き価格、税込み価格、税金を表示する。
	    }
	    
	    private static int tax(int price, int rate) {
	    	// 税金を計算する。
	    }
```

処理は正しく書けているのであと一歩です。


レビュー指摘事項の修正を確認しました。
消費税計算部分を正しくメソッドに切り出せていますね。

変数名もわかりやすくてGoodだと思います。
この調子で進めてください。

定数を使ってみたのも良い工夫だと思います。

なお、Javaでは変数名やメソッド名は小文字で始めるのが通例です。
コーディング規約などで指定がない限りは、上記の慣例にならうとよいです。

```Tax```→```tax```、```TaxMethod```→```calculateTax```など。

この調子で学習を進めてください。

レビュー指摘事項の修正を確認しました。消費税計算部分を正しくメソッドに切り出せていますね。

今後のために1点コメントします。
役割や用途が一目瞭然になるような、変数名やメソッド名を使いましょう。
また、アクセス範囲の修飾子は最小限にするのがポイントです。

例えば、以下メソッドの定義は、

```
public static int tax(int price4, double price5)
```

以下のようにするとベターです。

```
private static int calculateTax(int price, double taxRate)
```

* このクラス内に限って使うメソッドのため、アクセス修飾子をprivateにしました。

* 税金を計算するメソッドなので、calculateTaxというメソッド名にしました。

* 「金額」「税率」を表現するため、price, taxRateという引数名にしました。


結果は正しいのですが、メソッドへの切り出しが正しく行えていないように見えるので、再提出としました。

課題の意図としては、以下のようなコーディングを期待しています。

```
	public static void main(String[] args) {
		int price = 100; // 税抜金額
		// taxメソッドを呼び出し、税金を計算する。
		// 税抜金額と税金を足し、税込金額を計算する。
		// 税抜金額、税金、税込金額を表示する。
	}

	private static int tax(int price, int taxRate) {
		// 税抜金額priceと、税率taxRateにより、税金を計算する。
		// 計算した税金を返す。
	}
```

さて、わざわざメソッドに分ける意味ですが、いくつかあります。

* ソースコードが読みやすくなる。
* 保守性が向上する。
* 再利用しやすくなる。

現実の業務では、複数名でのコーディングが普通です。
処理を「意味のあるかたまり」に分けておく(今回の場合は「消費税を計算する」というかたまり)にしておくと、別人が読んだ場合に格段に理解が早まります。
結果的に、開発の効率化、バグの削減につながります。

課題を確認しました、OKです。
消費税計算部分を正しくメソッドに切り出せていますね。
変数名もわかりやすくてGoodだと思います。
この調子で進めてください。

再提出ありがとうございます、確認しました。
仕様を満足しておりOKです。
メソッドへの切り出しを正しく行えましたね。
